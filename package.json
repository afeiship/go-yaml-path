{
  "name": "@jswork/go-yaml-path",
  "private": true,
  "version": "1.0.3",
  "description": "YAML config access via hierarchical dot paths.",
  "license": "MIT",
  "scripts": {
    "test": "go test tests/*.go -v",
    "release": "release-it"
  }
}

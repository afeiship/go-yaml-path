{
  "name": "@jswork/go-yaml-path",
  "private": true,
  "version": "1.0.1",
  "description": "YAML config access via hierarchical dot paths.",
  "license": "MIT",
  "scripts": {
    "test": "go test *.go -v",
    "release": "release-it"
  }
}
